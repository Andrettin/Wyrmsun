LoadTileModels("scripts/tilesets/swamp.lua")

if (LoadedGame == false) then
	GenerateRandomMap({
		Symmetric = true,
		MixedCivilizations = true,
		MercenaryCamp = true,
		RockQuantity = "low",
		TreeQuantity = "medium",
		WaterQuantity = "low",
		WorkerQuantity = 5
	})
end