LoadTileModels("scripts/tilesets/swamp.lua")

if (LoadedGame == false) then
	GenerateRandomMap({
		RockQuantity = "high",
		TreeQuantity = "low",
		WaterQuantity = "low",
		NoFlyingCreeps = true,
		WorkerQuantity = 3,
		NoDeposits = true
	})
end