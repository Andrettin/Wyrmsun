haban = {
	name = "[word:haban.name.normalized]"
	icon = icon_teuton_dock
	player_color = red
	description = "\"[word:haban.name.normalized]\" means \"[word:haban.meanings.1.lowered]\" in Old Saxon. Building a dock enables the construction of ships to sail through seas and rivers."
	hint = "Select a [civilization:teutonic.class_unit_type:worker.name], press the Build Structure button and then click on the [civilization:teutonic.class_unit_type:dock.name] button to build the structure."
	conditions = {
		scripted_condition = has_old_saxon_culture
		coastal = true
	}
	objectives = {
		build_units = {
			unit_class = dock
		}
	}
	completion_effects = {
		copper += 250
		lumber += 250
	}
}
