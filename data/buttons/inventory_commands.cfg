[button]
	pos = 14
	icon = icon_inventory
	action = button
	value = inventory_level
	allowed = check_has_inventory
	key = i
	hint = "Open ~!Inventory"
	popup = popup_commands
	for_unit_classes += infantry
	for_unit_classes += veteran_infantry
	for_unit_classes += heroic_infantry
	for_unit_classes += spearman
	for_unit_classes += veteran_spearman
	for_unit_classes += heroic_spearman
	for_unit_classes += shooter
	for_unit_classes += veteran_shooter
	for_unit_classes += heroic_shooter
	for_unit_classes += gunpowder_infantry
	for_unit_classes += cavalry
	for_unit_classes += heroic_cavalry
	for_unit_classes += desert_cavalry
	for_unit_classes += heroic_desert_cavalry
	for_unit_classes += flying_rider
	for_unit_classes += thief
	for_unit_classes += priest
	for_unit_classes += veteran_priest
	for_unit_classes += high_priest
	for_unit_classes += geomancer
	for_unit_classes += merchant
	for_unit_classes += heroic_merchant
	for_units += unit_wyrm
[/button]

[button]
	pos = 15
	icon = icon_inventory
	action = button
	value = inventory_level
	allowed = check_has_inventory
	key = i
	hint = "Open ~!Inventory"
	popup = popup_commands
	for_unit_classes += worker
	for_unit_classes += skilled_miner
	for_unit_classes += expert_miner
	for_unit_classes += journeyman_smith
	for_unit_classes += master_smith
	for_unit_classes += grandmaster_smith
[/button]

[button]
	pos = 16
	level = inventory_level
	icon = icon_cancel
	action = button
	value = ""
	key = escape
	hint = "Cancel (~<Esc~>)"
	popup = popup_commands
	for_unit_classes += worker
	for_unit_classes += skilled_miner
	for_unit_classes += expert_miner
	for_unit_classes += journeyman_smith
	for_unit_classes += master_smith
	for_unit_classes += grandmaster_smith
	for_unit_classes += infantry
	for_unit_classes += veteran_infantry
	for_unit_classes += heroic_infantry
	for_unit_classes += spearman
	for_unit_classes += veteran_spearman
	for_unit_classes += heroic_spearman
	for_unit_classes += shooter
	for_unit_classes += veteran_shooter
	for_unit_classes += heroic_shooter
	for_unit_classes += gunpowder_infantry
	for_unit_classes += cavalry
	for_unit_classes += heroic_cavalry
	for_unit_classes += desert_cavalry
	for_unit_classes += heroic_desert_cavalry
	for_unit_classes += flying_rider
	for_unit_classes += thief
	for_unit_classes += priest
	for_unit_classes += veteran_priest
	for_unit_classes += high_priest
	for_unit_classes += geomancer
	for_unit_classes += merchant
	for_unit_classes += heroic_merchant
	for_units += unit_wyrm
[/button]
