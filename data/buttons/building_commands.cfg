[button]
	pos = 1
	icon = icon_enter
	action = enter_map_layer
	key = e
	hint = "~!Enter Map Layer"
	popup = popup_commands
	always_show = true
	for_units += unit_cavern_entrance
	for_units += unit_portal
[/button]

[button]
	pos = 13
	icon = icon_rally_point
	action = rally_point
	key = r
	hint = "Set ~!Rally Point"
	popup = popup_commands
	for_unit_classes += town_hall
	for_unit_classes += stronghold
	for_unit_classes += fortress
	for_unit_classes += barracks
	for_unit_classes += temple
	for_unit_classes += dock
	for_unit_classes += market
	for_unit_classes += watch_tower
	for_unit_classes += guard_tower
[/button]

[button]
	pos = 14
	icon = icon_crown
	action = button
	value = found_faction_level
	allowed = check_has_sub_buttons
	key = f
	hint = "~!Found a Faction"
	popup = popup_commands
	for_unit_classes += town_hall
	for_unit_classes += stronghold
	for_unit_classes += fortress
[/button]

[button]
	pos = 15
	icon = icon_book_red
	action = button
	value = enact_law_level
	allowed = check_has_sub_buttons
	key = l
	hint = "Enact a ~!Law"
	popup = popup_commands
	for_unit_classes += town_hall
	for_unit_classes += stronghold
	for_unit_classes += fortress
[/button]

[button]
	pos = 16
	icon = icon_salvage
	action = salvage
	key = delete
	hint = "Salvage Building (~<Del~>)"
	popup = popup_commands
	for_unit_classes += town_hall
	#for_unit_classes += stronghold #cannot have this for strongholds because it conflicts with the garrison button
	#for_unit_classes += fortress #cannot have this for fortresses because it conflicts with the garrison button
	for_unit_classes += farm
	for_unit_classes += barracks
	for_unit_classes += lumber_mill
	for_unit_classes += smithy
	for_unit_classes += masons_shop
	for_unit_classes += stables
	for_unit_classes += market
	for_unit_classes += temple
	for_unit_classes += university
	for_unit_classes += watch_tower
	for_unit_classes += guard_tower
	for_unit_classes += heavy_tower
	for_unit_classes += cannon_tower
	for_unit_classes += dock
	for_unit_classes += banner
	for_unit_classes += gold_mine
	for_unit_classes += silver_mine
	for_unit_classes += copper_mine
	for_unit_classes += iron_mine
	for_unit_classes += mithril_mine
	for_unit_classes += coal_mine
	for_unit_classes += diamond_mine
	for_unit_classes += emerald_mine
	for_unit_classes += yale_hunting_lodge
	for_unit_classes += mercenary_camp
[/button]

[button]
	pos = 16
	level = found_faction_level
	icon = icon_cancel
	action = button
	value = ""
	key = escape
	hint = "Cancel (~<Esc~>)"
	popup = popup_commands
	for_unit_classes += town_hall
	for_unit_classes += stronghold
	for_unit_classes += fortress
[/button]

[button]
	pos = 16
	level = found_faction_level_2
	icon = icon_cancel
	action = button
	value = ""
	key = escape
	hint = "Cancel (~<Esc~>)"
	popup = popup_commands
	for_unit_classes += town_hall
	for_unit_classes += stronghold
	for_unit_classes += fortress
[/button]

[button]
	pos = 16
	level = enact_law_level
	icon = icon_cancel
	action = button
	value = ""
	key = escape
	hint = "Cancel (~<Esc~>)"
	popup = popup_commands
	for_unit_classes += town_hall
	for_unit_classes += stronghold
	for_unit_classes += fortress
[/button]
